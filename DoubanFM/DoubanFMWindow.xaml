<local:WindowBase
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:DoubanFM"  xmlns:core="clr-namespace:DoubanFM.Core;assembly=DoubanFM.Core"  mc:Ignorable="d" x:Class="DoubanFM.DoubanFMWindow" x:Name="Window"
		Title="豆瓣电台" ResizeMode="CanMinimize" Closed="Window_Closed" WindowStartupLocation="CenterScreen" IsVisibleChanged="Window_IsVisibleChanged" AllowsTransparency="{Binding Source={StaticResource Player}, Path=Settings.IsShadowEnabled}" KeyUp="Window_KeyUp" Background="#FF1960AF" Loaded="Window_Loaded" Topmost="{Binding Source={StaticResource Player}, Path=Settings.TopMost}" TextOptions.TextFormattingMode="Ideal">
	<Window.Resources>
		<Storyboard x:Key="BackgroundColorStoryboard">
			<ColorAnimation Storyboard.TargetName="Window" Storyboard.TargetProperty="(Control.Background).(SolidColorBrush.Color)" Duration="0:0:5" EasingFunction="{StaticResource Smooth}"/>
		</Storyboard>
		<Storyboard x:Key="ShowCover1Storyboard">
			<DoubleAnimation Storyboard.TargetName="Cover1" Storyboard.TargetProperty="Opacity" Duration="0:0:1" To="1"/>
		</Storyboard>
		<Storyboard x:Key="ShowCover2Storyboard">
			<DoubleAnimation Storyboard.TargetName="Cover1" Storyboard.TargetProperty="Opacity" Duration="0:0:1" To="0"/>
		</Storyboard>
		<Storyboard x:Key="SlideCoverRightStoryboard">
			<ThicknessAnimation Storyboard.TargetName="CoverGrid" Storyboard.TargetProperty="Margin"
			 To="300,0,0,0" Duration="0:0:0.5" EasingFunction="{StaticResource Smooth}"/>
		</Storyboard>
		<Storyboard x:Key="SlideCoverLeftStoryboard">
			<ThicknessAnimation Storyboard.TargetName="CoverGrid" Storyboard.TargetProperty="Margin"
			 To="0,0,0,0" Duration="0:0:0.5" EasingFunction="{StaticResource Smooth}"/>
		</Storyboard>
		<Storyboard x:Key="ChangeSongInfoStoryboard">
			<DoubleAnimationUsingKeyFrames Storyboard.TargetName="SongInfoPanel" Storyboard.TargetProperty="Opacity">
				<LinearDoubleKeyFrame Value="0" KeyTime="0:0:0.3"/>
				<EasingDoubleKeyFrame Value="1" KeyTime="0:0:0.6" EasingFunction="{StaticResource Smooth}"/>
			</DoubleAnimationUsingKeyFrames>
			<StringAnimationUsingKeyFrames Storyboard.TargetName="SongTitle" Storyboard.TargetProperty="Content">
				<DiscreteStringKeyFrame KeyTime="0:0:0.3"/>
			</StringAnimationUsingKeyFrames>
			<StringAnimationUsingKeyFrames Storyboard.TargetName="Artist" Storyboard.TargetProperty="Content">
				<DiscreteStringKeyFrame KeyTime="0:0:0.3"/>
			</StringAnimationUsingKeyFrames>
			<StringAnimationUsingKeyFrames Storyboard.TargetName="Album" Storyboard.TargetProperty="Content">
				<DiscreteStringKeyFrame KeyTime="0:0:0.3"/>
			</StringAnimationUsingKeyFrames>
		</Storyboard>
		<Storyboard x:Key="DjChannelClickStoryboard">
			<BooleanAnimationUsingKeyFrames Storyboard.TargetName="DjCates" Storyboard.TargetProperty="IsEnabled">
				<DiscreteBooleanKeyFrame KeyTime="0" Value="False"/>
			</BooleanAnimationUsingKeyFrames>
			<ThicknessAnimation
Storyboard.TargetName="ChannelsContentGrid" Storyboard.TargetProperty="Margin" To="-750,0,0,0" Duration="0:0:0.5" EasingFunction="{StaticResource Smooth}"/>
		</Storyboard>
		<BitmapImage x:Key="PauseThumbImage">Images/DoubanFM_Pause.png</BitmapImage>
		<BitmapImage x:Key="PlayThumbImage">Images/DoubanFM_Play.png</BitmapImage>
		<BitmapImage x:Key="NextThumbImage">Images/DoubanFM_Next.png</BitmapImage>
		<BitmapImage x:Key="LikeThumbImage">Images/DoubanFM_Like_Like.png</BitmapImage>
		<BitmapImage x:Key="LikeThumbImage_Disabled">Images/DoubanFM_Like_Disabled.png</BitmapImage>
		<BitmapImage x:Key="UnlikeThumbImage">Images/DoubanFM_Like_Unlike.png</BitmapImage>
		<BitmapImage x:Key="NeverThumbImage">Images/DoubanFM_Never.png</BitmapImage>
		<BitmapImage x:Key="NeverThumbImage_Disabled">Images/DoubanFM_Never_Disabled.png</BitmapImage>
	</Window.Resources>
	<Window.Triggers>
		<EventTrigger RoutedEvent="Mouse.MouseEnter" SourceName="SharePanel"/>
	</Window.Triggers>
	<Window.TaskbarItemInfo>
		<TaskbarItemInfo>
			<TaskbarItemInfo.ThumbButtonInfos>
				<ThumbButtonInfo x:Name="LikeThumb" ImageSource="{StaticResource UnlikeThumbImage}" Click="LikeThumb_Click" Description="喜欢" />
				<ThumbButtonInfo x:Name="NeverThumb" ImageSource="{StaticResource NeverThumbImage_Disabled}" Click="NeverThumb_Click" Description="不再播放" IsEnabled="False"/>
				<ThumbButtonInfo x:Name="PauseThumb" Click="PauseThumb_Click" ImageSource="{StaticResource PauseThumbImage}" Description="暂停" />
				<ThumbButtonInfo x:Name="NextThumb" Click="NextThumb_Click" ImageSource="{StaticResource NextThumbImage}" Description="下一首" />
			</TaskbarItemInfo.ThumbButtonInfos>
		</TaskbarItemInfo>
	</Window.TaskbarItemInfo>
	<Grid>
		<Grid.LayoutTransform>
			<TransformGroup>
				<ScaleTransform ScaleX="{Binding Settings.ScaleTransform, Source={StaticResource Player}}" ScaleY="{Binding ScaleX, RelativeSource={RelativeSource Self}}"/>
			</TransformGroup>
		</Grid.LayoutTransform>
		<Grid x:Name="grid" Height="300">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="300" />
				<ColumnDefinition Width="300"/>
			</Grid.ColumnDefinitions>
			<Grid x:Name="LeftPanel" Background="Transparent" Panel.ZIndex="1">
				<TabControl x:Name="ControlPanel" Style="{DynamicResource ControlPaneStyle}" Foreground="{Binding Background, ElementName=Window}" ClipToBounds="True" FocusVisualStyle="{x:Null}">
					<TabItem Header="{DynamicResource TabItemChannels}" IsEnabled="{Binding Source={StaticResource Player}, Path=IsInitialized}">
						<Grid x:Name="ChannelsGrid">
							<Grid.RowDefinitions>
								<RowDefinition Height="40"/>
								<RowDefinition/>
							</Grid.RowDefinitions>
							<Grid Background="#4E000000">
								<Grid.ColumnDefinitions>
									<ColumnDefinition/>
									<ColumnDefinition/>
									<ColumnDefinition/>
								</Grid.ColumnDefinitions>
								<RadioButton x:Name="ButtonPersonal" Margin="5" Content="私人兆赫" IsEnabled="{Binding Source={StaticResource Player}, Path=UserAssistant.IsLoggedOn}" Style="{DynamicResource ChannelCategoryStyle}">
									<RadioButton.Triggers>
										<EventTrigger RoutedEvent="RadioButton.Checked">
											<BeginStoryboard>
												<Storyboard x:Name="PersonalClickStoryboard">
													<ThicknessAnimation
														Storyboard.TargetName="ChannelsContentGrid" Storyboard.TargetProperty="Margin" To="0" Duration="0:0:0.5" EasingFunction="{StaticResource Smooth}"/>
												</Storyboard>
											</BeginStoryboard>
										</EventTrigger>
									</RadioButton.Triggers>
								</RadioButton>
								<RadioButton x:Name="ButtonPublic" Grid.Column="1" Margin="5" Content="公共兆赫" Style="{DynamicResource ChannelCategoryStyle}">
									<RadioButton.Triggers>
										<EventTrigger RoutedEvent="RadioButton.Checked">
											<BeginStoryboard>
												<Storyboard x:Name="PublicClickStoryboard">
													<ThicknessAnimation
														Storyboard.TargetName="ChannelsContentGrid" Storyboard.TargetProperty="Margin" To="-250,0,0,0" Duration="0:0:0.5" EasingFunction="{StaticResource Smooth}"/>
												</Storyboard>
											</BeginStoryboard>
										</EventTrigger>
									</RadioButton.Triggers>
								</RadioButton>
								<RadioButton x:Name="ButtonDjCates" Grid.Column="2" Margin="5" Content="DJ兆赫" Click="ButtonDjCates_Click" Style="{DynamicResource ChannelCategoryStyle}">
									<RadioButton.Triggers>
										<EventTrigger RoutedEvent="RadioButton.Checked">
											<BeginStoryboard>
												<Storyboard x:Name="DjClickStoryboard">
													<BooleanAnimationUsingKeyFrames Storyboard.TargetName="DjCates" Storyboard.TargetProperty="IsEnabled">
														<DiscreteBooleanKeyFrame KeyTime="0" Value="True"/>
													</BooleanAnimationUsingKeyFrames>
													<ThicknessAnimation
														Storyboard.TargetName="ChannelsContentGrid" Storyboard.TargetProperty="Margin" To="-500,0,0,0" Duration="0:0:0.5" EasingFunction="{StaticResource Smooth}"/>
												</Storyboard>
											</BeginStoryboard>
										</EventTrigger>
									</RadioButton.Triggers>
								</RadioButton>
							</Grid>
							<Grid x:Name="ChannelsContentGrid" Grid.Row="1" Width="1000" d:IsHidden="True">
								<Grid.ColumnDefinitions>
									<ColumnDefinition/>
									<ColumnDefinition/>
									<ColumnDefinition/>
									<ColumnDefinition/>
								</Grid.ColumnDefinitions>
								<ListBox x:Name="PersonalChannels" SelectionChanged="PersonalChannels_SelectionChanged" Style="{DynamicResource ChannelsStyle}">
									<ListBox.ItemTemplate>
										<DataTemplate>
											<TextBlock Text="{Binding Name}" FontSize="20" TextWrapping="Wrap" Padding="10"/>	
										</DataTemplate>
									</ListBox.ItemTemplate>
									<ListBox.ItemsPanel>
										<ItemsPanelTemplate>
											<UniformGrid Margin="50"/>
										</ItemsPanelTemplate>
									</ListBox.ItemsPanel>
								</ListBox>
								<ListBox Grid.Column="1" x:Name="PublicChannels" SelectionChanged="PublicChannels_SelectionChanged" Style="{DynamicResource ChannelsStyle}">
									<ListBox.ItemTemplate>
										<DataTemplate>
											<TextBlock x:Name="TextBlockDjChannel" Text="{Binding Name}" FontSize="20" TextWrapping="Wrap" Padding="10"/>
										</DataTemplate>
									</ListBox.ItemTemplate>
								</ListBox>
								<ListBox Grid.Column="2" x:Name="DjCates" Style="{DynamicResource ChannelsStyle}" SelectionChanged="DjCates_SelectionChanged">
									<ListBox.ItemTemplate>
										<DataTemplate>
											<TextBlock Text="{Binding Name}" FontSize="20" TextWrapping="Wrap" Padding="10"/>
										</DataTemplate>
									</ListBox.ItemTemplate>
								</ListBox>
								<ListBox Grid.Column="3" x:Name="DjChannels" Style="{DynamicResource ChannelsStyle}" SelectionChanged="DjChannels_SelectionChanged">
									<ListBox.ItemTemplate>
										<DataTemplate>
											<TextBlock Text="{Binding Name}" FontSize="20" TextWrapping="Wrap" Padding="10"/>
										</DataTemplate>
									</ListBox.ItemTemplate>
								</ListBox>
							</Grid>
						</Grid>
					</TabItem>
					<TabItem Header="{DynamicResource TabItemSearch}" IsEnabled="{Binding Source={StaticResource Player}, Path=IsInitialized}">
						<Grid>
							<Grid.RowDefinitions>
								<RowDefinition Height="Auto"/>
								<RowDefinition/>
								<RowDefinition Height="40"/>
							</Grid.RowDefinitions>
							<Grid Margin="10" Height="30">
								<Grid.ColumnDefinitions>
									<ColumnDefinition/>
									<ColumnDefinition Width="Auto"/>
								</Grid.ColumnDefinitions>
								<TextBox x:Name="SearchText" Height="20" IsEnabled="{Binding Source={StaticResource Player}, Path=MusicSearch.IsSearchFinished}"/>
								<Button x:Name="Search" Grid.Column="1" Margin="10,0,0,0" Click="Search_Click" IsDefault="True" IsEnabled="{Binding Source={StaticResource Player}, Path=MusicSearch.IsSearchFinished}" Style="{DynamicResource SearchStyle}"/>
							</Grid>
							<ListBox x:Name="SearchResultList" Grid.Row="1" Style="{DynamicResource SearchResultListStyle}" SelectionChanged="SearchResultList_SelectionChanged" ItemsSource="{Binding Source={StaticResource Player}, Path=MusicSearch.SearchResult}" />
							<Label Grid.Row="1" Visibility="{Binding Converter={StaticResource BoolReverseToVisibilityConverter}, Source={StaticResource Player}, Path=MusicSearch.IsSearchFinished}" HorizontalAlignment="Center" VerticalAlignment="Center">正在搜索...</Label>
							<Grid Grid.Row="1" Visibility="{Binding Path=MusicSearch.ShowNoResultHint, Source={StaticResource Player}, Converter={StaticResource BooleanToVisibilityConverter}}" Height="Auto" VerticalAlignment="Center">
								<Grid.RowDefinitions>
									<RowDefinition/>
									<RowDefinition/>
								</Grid.RowDefinitions>
								<Label HorizontalAlignment="Center" VerticalAlignment="Center">没有任何搜索结果</Label>
								<Label Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">请不要输入过短或过长的内容</Label>
							</Grid>
							<StackPanel Grid.Row="2" HorizontalAlignment="Center" Orientation="Horizontal" Height="30">
								<Button x:Name="PreviousPage" Click="PreviousPage_Click" IsEnabled="{Binding Path=MusicSearch.IsPreviousPageEnabled, Source={StaticResource Player}}" Style="{DynamicResource PreviousPageStyle}"/>
								<Border Width="20"/>
								<Button x:Name="NextPage" Click="NextPage_Click" IsEnabled="{Binding Path=MusicSearch.IsNextPageEnabled, Source={StaticResource Player}}" Style="{DynamicResource NextPageStyle}"/>
							</StackPanel>
						</Grid>
					</TabItem>
					<TabItem x:Name="Account" Header="{DynamicResource TabItemAccount}" IsEnabled="{Binding Source={StaticResource Player}, Path=IsInitialized}">
						<Grid>
							<Grid x:Name="LogOnPanel" Visibility="{Binding Converter={StaticResource BooleanToVisibilityConverter}, Path=UserAssistant.IsLoggedOff, Source={StaticResource Player}}">
								<Grid Margin="20">
									<Grid.RowDefinitions>
										<RowDefinition Height="Auto"/>
										<RowDefinition Height="Auto"/>
										<RowDefinition/>
									</Grid.RowDefinitions>
									<Grid>
										<Grid.RowDefinitions>
											<RowDefinition/> 					
											<RowDefinition/> 					
											<RowDefinition Height="Auto"/> 					
											<RowDefinition Height="Auto"/> 					
										</Grid.RowDefinitions> 
										<Grid.ColumnDefinitions>
											<ColumnDefinition Width="Auto"/>
											<ColumnDefinition/>
										</Grid.ColumnDefinitions>
										<Label HorizontalAlignment="Right" VerticalAlignment="Center" Content="邮箱"/>
										<Label HorizontalAlignment="Right" VerticalAlignment="Center" Grid.Row="1" Content="密码"/>
										<TextBox x:Name="TbUsername" Grid.Column="1" Margin="4" Text="{Binding Path=Settings.User.Username, Mode=TwoWay, Source={StaticResource Player}}"/>
										<PasswordBox x:Name="PbPassword" Grid.Row="1" Grid.Column="1" Margin="4" PasswordChanged="PbPassword_PasswordChanged" />
										<Grid x:Name="CaptchaGrid" Grid.Row="2" Grid.ColumnSpan="2" Margin="0,10" Visibility="{Binding Converter={StaticResource BooleanToVisibilityConverter}, Path=UserAssistant.HasCaptcha, Source={StaticResource Player}}">
											<Grid.ColumnDefinitions>
												<ColumnDefinition Width="Auto"/>
												<ColumnDefinition/>
											</Grid.ColumnDefinitions>
											<Grid.RowDefinitions>
												<RowDefinition Height="Auto"/>
												<RowDefinition/>
											</Grid.RowDefinitions>
											<Button x:Name="ButtonRefreshCaptcha" Grid.ColumnSpan="2" Click="ButtonRefreshCaptcha_Click" Padding="0" MinHeight="40" MaxHeight="40" BorderThickness="0">
												<Image x:Name="Captcha" Stretch="Fill"/>
											</Button>
											<Label Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Center" Content="验证码"/>
											<TextBox x:Name="CaptchaText" Grid.Row="1" Grid.Column="1" Margin="4"/>
										</Grid>
									</Grid>
									<Grid Grid.Row="1">
										<Grid.ColumnDefinitions>
											<ColumnDefinition/>
											<ColumnDefinition/>
										</Grid.ColumnDefinitions>
										<Label x:Name="LogOnFailedHint" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding Converter={StaticResource BooleanToVisibilityConverter}, Path=UserAssistant.ShowLogOnFailedHint, Source={StaticResource Player}}" Content="登录失败！"/>
										<Button Grid.Column="1" x:Name="ButtonLogOn" Margin="10" Click="ButtonLogOn_Click" IsDefault="True" Content="登录"/>
									</Grid>
									<Grid Grid.Row="2" Margin="0,20">
										<Grid.ColumnDefinitions>
											<ColumnDefinition/>
											<ColumnDefinition/>
										</Grid.ColumnDefinitions>
										<CheckBox x:Name="CheckBoxRememberPassword" VerticalAlignment="Center" HorizontalAlignment="Center" Content="记住密码" IsChecked="{Binding Path=Settings.RememberPassword, Mode=TwoWay, Source={StaticResource Player}}" />
										<CheckBox Grid.Column="1" x:Name="CheckBoxAutoLogOnNextTime" VerticalAlignment="Center" HorizontalAlignment="Center" Content="下次自动登录" IsChecked="{Binding Path=Settings.AutoLogOnNextTime, Mode=TwoWay, Source={StaticResource Player}}" />
									</Grid>
								</Grid>
							</Grid>
							<Grid x:Name="LogOffPanel" Visibility="{Binding Converter={StaticResource BooleanToVisibilityConverter}, Path=UserAssistant.IsLoggedOn, Source={StaticResource Player}}">
								<Grid.RowDefinitions>
									<RowDefinition/>
									<RowDefinition/>
									<RowDefinition/>
								</Grid.RowDefinitions>
								<Label Margin="10" HorizontalAlignment="Center" VerticalAlignment="Center" Content="{Binding Text, ElementName=TbUsername}"/>
								<Label Grid.Row="1" Margin="10" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="30" Content="已登录"/>
								<Button Grid.Row="2" x:Name="ButtonLogOff" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="20,5" Margin="10" Click="ButtonLogOff_Click" Content="注销"/>
							</Grid>
							<Grid x:Name="LoggingOnPanel" Visibility="{Binding Converter={StaticResource BooleanToVisibilityConverter}, Path=UserAssistant.IsLoggingOn, Source={StaticResource Player}}">
								<Label FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center" Content="正在登录"/>
							</Grid>
							<Grid x:Name="LoggingOffPanel" Visibility="{Binding Converter={StaticResource BooleanToVisibilityConverter}, Path=UserAssistant.IsLoggingOff, Source={StaticResource Player}}">
								<Label FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center" Content="正在注销"/>
							</Grid>
						</Grid>
					</TabItem>
					<TabItem Header="{DynamicResource TabItemSettings}">
						<Grid Margin="20">
							<Grid.RowDefinitions>
								<RowDefinition Height="Auto"/>
								<RowDefinition Height="Auto"/>
							</Grid.RowDefinitions>
							<local:VolumeControl x:Name="VolumeControl" IsMuted="{Binding Settings.IsMuted, Mode=TwoWay, Source={StaticResource Player}}" Volume="{Binding Settings.Volume, Mode=TwoWay, Source={StaticResource Player}}" Margin="5"/>
							<Grid Grid.Row="1" Width="100" Margin="10">
								<Grid.RowDefinitions>
									<RowDefinition/>
									<RowDefinition Height="10"/>
									<RowDefinition/>
									<RowDefinition Height="10"/>
									<RowDefinition/>
									<RowDefinition Height="10"/>
									<RowDefinition/>
									<RowDefinition Height="10"/>
									<RowDefinition/>
								</Grid.RowDefinitions>
								<Button x:Name="ButtonGeneralSetting" Content="常规设置" Click="ButtonGeneralSetting_Click"/>
								<Button Grid.Row="2" x:Name="ButtonUISetting" Content="界面设置" Click="ButtonUISetting_Click"/>
								<Button Grid.Row="4" x:Name="ButtonLyricsSetting" Content="歌词设置" Click="LyricsSetting_Click" />
								<Button Grid.Row="6" x:Name="ButtonHotKeySettings" Click="ButtonHotKeySettings_Click" Content="热键设置"/>
								<Button Grid.Row="8" x:Name="ButtonShareSetting" Content="分享设置" Click="ButtonShareSetting_Click"/>
							</Grid>
						</Grid>			
					</TabItem>
					<TabItem Header="{DynamicResource TabItemInfo}">
						<Border Padding="20">
							<Grid>
								<Grid.RowDefinitions>
									<RowDefinition Height="Auto"/>
									<RowDefinition Height="Auto"/>
									<RowDefinition/>
									<RowDefinition/>
									<RowDefinition Height="Auto"/>
									<RowDefinition Height="Auto"/>
								</Grid.RowDefinitions>
								<StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
									<Button x:Name="GoToHomePage" Height="40" Style="{DynamicResource IconStyle}" Margin="0,0,10,0" Click="GoToHomePage_Click"/>
									<Label HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="26.667">豆瓣电台</Label>
								</StackPanel>
								<Label Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,10">1.5.0</Label>
								<Button x:Name="CheckUpdate" Grid.Row="2" Click="CheckUpdate_Click" Style="{DynamicResource CheckUpdateStyle}"/>
								<Grid Grid.Row="3" Height="40">
									<Grid.ColumnDefinitions>
										<ColumnDefinition/>
										<ColumnDefinition/>
									</Grid.ColumnDefinitions>
									<Button x:Name="Feedback" Click="Feedback_Click" Style="{DynamicResource FeedbackStyle}"/>
									<Button x:Name="VisitOfficialWebsite" Grid.Column="1" Click="VisitOfficialWebsite_Click" Style="{DynamicResource DoubanFMOfficialWebsiteStyle}"/>
								</Grid>
								<Label Grid.Row="4" HorizontalAlignment="Center" VerticalAlignment="Center">Copyright © 2011 K.F.Storm.</Label>
								<Label Grid.Row="5" HorizontalAlignment="Center" VerticalAlignment="Center">All rights reserved.</Label>
							</Grid>
						</Border>
					</TabItem>
				</TabControl>
			</Grid>
			<Grid x:Name="RightPanel" Grid.Column="1" Background="Transparent">
				<Grid Margin="10">
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="3*"/>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="Auto"/>
						<RowDefinition Height="Auto"/>
						<RowDefinition/>
					</Grid.RowDefinitions>
					<Grid Height="20">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="2*"/>
							<ColumnDefinition/>
						</Grid.ColumnDefinitions>
						<TextBlock x:Name="ChannelTextBlock" TextTrimming="CharacterEllipsis" Foreground="White" Margin="10,0,0,0" FontSize="14.667" FontStretch="Normal" Text="{Binding Source={StaticResource Player}, Path=CurrentChannel}" />
						<StackPanel Grid.Column="1" x:Name="WindowStateControls" Orientation="Horizontal" HorizontalAlignment="Right">
							<Button x:Name="ButtonMinimize" Click="ButtonMinimize_Click" Style="{DynamicResource MinimizeStyle}"/>
							<Border Width="10"/>
							<Button x:Name="ButtonToNotifyIcon" Click="ButtonToNotifyIcon_Click" Style="{DynamicResource ToNotifyIconStyle}"/>
							<Border Width="10"/>
							<Button x:Name="ButtonExit" Click="ButtonExit_Click" Style="{DynamicResource ExitStyle}"/>
						</StackPanel>
					</Grid>
					<Grid Grid.Row="1" x:Name="SongInfoPanel">
						<Grid.RowDefinitions>
							<RowDefinition Height="50"/>
							<RowDefinition/>
							<RowDefinition/>
						</Grid.RowDefinitions>
						<Viewbox HorizontalAlignment="Left" StretchDirection="DownOnly">
							<Label x:Name="SongTitle" FontSize="31" VerticalContentAlignment="Center" Content="豆瓣电台" Foreground="White"/>
						</Viewbox>
						<Viewbox Margin="0,5" Grid.Row="1" HorizontalAlignment="Left" StretchDirection="DownOnly">
							<Label x:Name="Artist" FontSize="22" VerticalContentAlignment="Center" Foreground="White" />
						</Viewbox>
						<Viewbox Margin="0,5" Grid.Row="2" HorizontalAlignment="Left" StretchDirection="DownOnly">
							<Label x:Name="Album" VerticalContentAlignment="Center" FontSize="22" Foreground="White" />
						</Viewbox>
					</Grid>
					<StackPanel Grid.Row="2" HorizontalAlignment="Right" Orientation="Horizontal" Height="24">
						<Grid x:Name="SharePanel">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="Auto"/>
								<ColumnDefinition Width="Auto"/>
							</Grid.ColumnDefinitions>
							<Grid.Triggers>
								<EventTrigger RoutedEvent="UIElement.MouseEnter">
									<BeginStoryboard>
										<Storyboard>
											<DoubleAnimationUsingKeyFrames  Storyboard.TargetName="Shares" Storyboard.TargetProperty="Width">
												<DiscreteDoubleKeyFrame KeyTime="0" Value="NaN"/>
											</DoubleAnimationUsingKeyFrames>
											<DoubleAnimation Storyboard.TargetName="Shares" Storyboard.TargetProperty="Opacity" To="1" EasingFunction="{StaticResource Smooth}" Duration="0:0:0.5"/>
										</Storyboard>
									</BeginStoryboard>
								</EventTrigger>
								<EventTrigger RoutedEvent="UIElement.MouseLeave">
									<BeginStoryboard>
										<Storyboard>
											<DoubleAnimationUsingKeyFrames  Storyboard.TargetName="Shares" Storyboard.TargetProperty="Width">
												<DiscreteDoubleKeyFrame KeyTime="0:0:1" Value="0"/>
											</DoubleAnimationUsingKeyFrames>
											<DoubleAnimation Storyboard.TargetName="Shares" Storyboard.TargetProperty="Opacity" To="0" EasingFunction="{StaticResource Smooth}" Duration="0:0:1"/>
										</Storyboard>
									</BeginStoryboard>
								</EventTrigger>
							</Grid.Triggers>
							<StackPanel x:Name="Shares" Width="0" Opacity="0" Orientation="Horizontal">
								<Button Click="ShareButton_Click" Style="{DynamicResource ShareDoubanStyle}"
										Tag="{x:Static core:Share+Sites.Douban}"/>
								<Button Click="ShareButton_Click" Style="{DynamicResource ShareWeiboStyle}"
										Tag="{x:Static core:Share+Sites.Weibo}"/>
								<Button Click="ShareButton_Click" Style="{DynamicResource ShareMsnStyle}"
										Tag="{x:Static core:Share+Sites.Msn}"/>
								<Button Click="ShareButton_Click" Style="{DynamicResource ShareKaixinStyle}"
										Tag="{x:Static core:Share+Sites.Kaixin}"/>
								<Button Click="ShareButton_Click" Style="{DynamicResource ShareRenenStyle}"
										Tag="{x:Static core:Share+Sites.Renren}"/>
								<Button Click="ShareButton_Click" Style="{DynamicResource ShareTencentWeiboStyle}"
										Tag="{x:Static core:Share+Sites.TencentWeibo}"/>
								<Button Click="ShareButton_Click" Style="{DynamicResource ShareFanfouStyle}"
										Tag="{x:Static core:Share+Sites.Fanfou}"/>
								<Button Click="ShareButton_Click" Style="{DynamicResource ShareFacebookStyle}"
										Tag="{x:Static core:Share+Sites.Facebook}"/>
								<Button Click="ShareButton_Click" Style="{DynamicResource ShareTwitterStyle}"
										Tag="{x:Static core:Share+Sites.Twitter}"/>
							</StackPanel>
							<Button x:Name="BtnCopyUrl" Grid.Column="1" Style="{StaticResource ShareStyle}" ToolTip="分享（点击直接复制地址）" Click="BtnCopyUrl_Click"/>
							<Button x:Name="BtnOneKeyShare" Grid.Column="1" Style="{StaticResource ShareStyle}" ToolTip="一键分享" Click="BtnOneKeyShare_Click"/>
						</Grid>
						<CheckBox x:Name="CheckBoxShowLyrics" IsChecked="{Binding Settings.ShowLyrics, Source={StaticResource Player}}" Checked="CheckBoxShowLyrics_Checked" Unchecked="CheckBoxShowLyrics_Unchecked" Style="{DynamicResource ShowLyricsStyle}"/>
					</StackPanel>
					<Grid Grid.Row="3" VerticalAlignment="Bottom">
						<Grid.ColumnDefinitions>
							<ColumnDefinition/>
							<ColumnDefinition/>
						</Grid.ColumnDefinitions>
						<Label x:Name="CurrentTime" FontSize="18" Foreground="White" Content="00:00"/>
						<Label Grid.Column="1" x:Name="TotalTime" FontSize="18" HorizontalAlignment="Right" Foreground="White" Content="00:00"/>
					</Grid>
					<Slider x:Name="Slider" Grid.Row="4" Margin="5,5" Style="{DynamicResource SliderStyleForProgress}" Background="#7F000000" Foreground="{Binding Background, Converter={StaticResource WindowBackgroundToSliderForegroundConverter}, ElementName=Window}"/>
					<Grid Grid.Row="5" HorizontalAlignment="Center" Height="50" VerticalAlignment="Bottom">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="38" />
							<ColumnDefinition Width="20"/>
							<ColumnDefinition Width="38" />
							<ColumnDefinition Width="20"/>
							<ColumnDefinition Width="32" />
							<ColumnDefinition Width="20"/>
							<ColumnDefinition Width="38"/>
						</Grid.ColumnDefinitions>
						<CheckBox Grid.Column="0" x:Name="CheckBoxLike" Style="{DynamicResource LikeStyle}" IsChecked="{Binding Path=IsLiked, Source={StaticResource Player}}" IsEnabled="{Binding Source={StaticResource Player}, Path=IsLikedEnabled}" />
						<Button Grid.Column="2" x:Name="ButtonNever" Style="{DynamicResource NeverStyle}" Click="ButtonNever_Click" IsEnabled="{Binding Source={StaticResource Player}, Path=IsNeverEnabled}" />
						<CheckBox Grid.Column="4" x:Name="CheckBoxPause" Style="{DynamicResource PauseStyle}" IsChecked="{Binding Path=IsPlaying, Source={StaticResource Player}, Converter={StaticResource BoolReverseConverter}}" />
						<Button Grid.Column="6" x:Name="ButtonNext" Click="ButtonNext_Click" Style="{DynamicResource NextStyle}"/>
					</Grid>
				</Grid>
			</Grid>
		</Grid>
		<Grid x:Name="CoverGrid" MouseLeftButtonDown="CoverGrid_MouseLeftButtonDown" Width="{Binding ActualWidth, ElementName=LeftPanel}" Height="{Binding ActualHeight, ElementName=LeftPanel}" HorizontalAlignment="Left" d:LayoutOverrides="HorizontalAlignment, GridBox" MouseMove="CoverGrid_MouseMove" MouseLeave="CoverGrid_MouseLeave" d:IsHidden="True">
			<MediaElement x:Name="Audio" LoadedBehavior="Manual" MediaEnded="Audio_MediaEnded" Volume="{Binding Settings.Volume, Mode=TwoWay, Source={StaticResource Player}}" IsMuted="{Binding Settings.IsMuted, Mode=TwoWay, Source={StaticResource Player}}" MediaFailed="Audio_MediaFailed" MediaOpened="Audio_MediaOpened" UnloadedBehavior="Manual" d:IsHidden="True" />
			<Image x:Name="Cover2" Stretch="UniformToFill"/>
			<Image x:Name="Cover1" Stretch="UniformToFill" Source="/DoubanFM;component/Images/DoubanFM_NoCover.png" />
			<Grid x:Name="LyricsPanel" Height="70" VerticalAlignment="Bottom" Visibility="Hidden">
				<Border Background="{Binding Background, ElementName=Window}">
					<Border.OpacityMask>
						<LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
							<GradientStop Offset="0"/>
							<GradientStop Color="#CC38FFA8" Offset="0.815"/>
							<GradientStop Color="#9047FFAE" Offset="0.422"/>
						</LinearGradientBrush>
					</Border.OpacityMask>
				</Border>
				<TextBlock x:Name="CurrentLyrics" Foreground="White" Margin="7" HorizontalAlignment="Center" VerticalAlignment="Bottom" TextWrapping="Wrap" TextTrimming="WordEllipsis"/>
			</Grid>
		</Grid>
	</Grid>
</local:WindowBase>
